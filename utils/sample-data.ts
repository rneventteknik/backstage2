import { User } from '../interfaces';

/** Dummy user data. */
export const sampleUserData: User[] = [
    { id: 101, name: 'Alice' } as User,
    { id: 102, name: 'Bob' } as User,
    { id: 103, name: 'Caroline' } as User,
    { id: 104, name: 'Dave' } as User,
];
